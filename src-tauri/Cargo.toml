[package]
name = "school-van-morgen"
version = "1.0.0"
description = "School van morgen — Rapportgenerator voor het basisonderwijs"
authors = ["School van morgen"]
edition = "2021"

[lib]
name = "school_van_morgen_lib"
crate-type = ["lib", "cdylib", "staticlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
opt-level = "s"
strip = true
